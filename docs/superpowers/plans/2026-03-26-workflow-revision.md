# Phase 3A Workflow Revision — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Revise the Phase 3A workflow docs based on findings from the first end-to-end test — adding media sourcing, approval gates, content persistence, correct build method, MCP schema reference, assessment guidance, audit re-run rules, and session boundary guidance.

**Architecture:** All changes are to markdown documentation files in `docs/phase-3/`. One new file (`step7-mcp-reference.md`) and updates to three existing files. No code changes.

**Tech Stack:** Markdown documentation. Source schemas in `mcp/src/lib/types.ts` and `mcp/src/tools/assessment.ts`.

**Spec:** `docs/superpowers/specs/2026-03-26-workflow-revision-design.md`

---

### Task 1: Create `step7-mcp-reference.md`

**Files:**
- Create: `docs/phase-3/step7-mcp-reference.md`

This is the largest new deliverable. Build it from the Zod schemas in `mcp/src/lib/types.ts` (block schemas, lines 316–586) and `mcp/src/tools/assessment.ts` (question input schemas, lines 7–58; `update_assessment_config`, lines 391–399).

- [ ] **Step 1: Write the content block reference section**

Create `docs/phase-3/step7-mcp-reference.md` with:

```markdown
# Step 7 — MCP Schema Reference

Load this at Step 7 (Build in TideLearn) before making any MCP tool calls.
Source of truth: Zod schemas in `mcp/src/lib/types.ts`. When in doubt, check the schema.

---

## Content block schemas — `add_block`

Omit `id` on all blocks and sub-items — auto-generated by `injectSubItemIds`.
Text fields accept HTML. Position is 1-based and optional (omit to append).

### Simple blocks

| Block | Fields (all required unless marked ?) |
|---|---|
| `heading` | `text: string` |
| `text` | `text: string` (HTML) |
| `divider` | *(no fields)* |
| `toc` | *(no fields)* |
| `image` | `src: string`, `alt: string` |
| `video` | `url: string` |
| `audio` | `src: string`, `title?: string` |
| `code` | `language: string`, `code: string` |
| `quote` | `text: string`, `cite?: string` |
| `document` | `src: string`, `fileType: "pdf"\|"docx"\|"xlsx"\|"pptx"`, `title?: string` |
| `button` | `label: string`, `url: string`, `variant: "primary"\|"secondary"\|"outline"`, `openInNewTab: boolean` |
| `embed` | `url: string`, `title: string`, `height: number` (100–2000) |

### Callout

```json
{
  "type": "callout",
  "variant": "info" | "success" | "warning" | "danger",  // REQUIRED
  "title": "optional title",
  "text": "callout body (HTML)"
}
```

⚠️ `variant` is required — omitting it fails validation.

### List

```json
{
  "type": "list",
  "style": "bulleted" | "numbered",
  "items": ["item1", "item2"]  // min 1 item, each min 1 char
}
```

### Accordion

```json
{
  "type": "accordion",
  "items": [
    { "title": "Panel title", "content": "Panel content (HTML)" }
  ]  // min 1 item. Omit id on each item.
}
```

### Tabs

```json
{
  "type": "tabs",
  "items": [
    { "label": "Tab label", "content": "Tab content (HTML)" }
  ]  // min 1 item. Field is "label" NOT "title". Omit id.
}
```

⚠️ Field is `label`, not `title`.

### Flashcard

```json
{
  "type": "flashcard",
  "front": "Question or term",
  "back": "Answer or definition",
  "hint": "optional hint"
}
```

### Timeline

```json
{
  "type": "timeline",
  "items": [
    { "date": "2024", "title": "Event title", "description": "optional" }
  ]  // min 2 items. Omit id.
}
```

### Process

```json
{
  "type": "process",
  "steps": [
    { "title": "Step title", "description": "optional" }
  ]  // min 2 steps. Omit id.
}
```

### Chart

```json
{
  "type": "chart",
  "chartType": "bar" | "line" | "pie",
  "title": "optional",
  "labels": ["A", "B", "C"],           // min 1
  "datasets": [
    { "label": "Series 1", "values": [10, 20, 30] }  // min 1 dataset, min 1 value
  ]
}
```

### Quiz (single MCQ)

```json
{
  "type": "quiz",
  "question": "Question text",
  "options": ["A", "B", "C", "D"],     // min 2 options
  "correctIndex": 0,                    // 0-based
  "showFeedback": true,                 // optional
  "feedbackMessage": "Explanation"       // optional — single field, not split correct/incorrect
}
```

⚠️ `correctIndex` is 0-based. Factory default is -1 — must be set.
⚠️ Feedback is a single `feedbackMessage` field, not `feedbackCorrect`/`feedbackIncorrect`.

### True/False

```json
{
  "type": "truefalse",
  "question": "Statement text",
  "correct": true,
  "showFeedback": true,                 // optional
  "feedbackCorrect": "Why correct",      // optional
  "feedbackIncorrect": "Why incorrect"   // optional
}
```

Note: `truefalse` uses split `feedbackCorrect`/`feedbackIncorrect`, unlike `quiz`.

### Short Answer

```json
{
  "type": "shortanswer",
  "question": "Question text",
  "answer": "expected answer",          // REQUIRED, min 1 char — cannot be empty string
  "acceptable": ["alt1", "alt2"],       // optional array of alternatives
  "caseSensitive": false,               // optional
  "trimWhitespace": true,               // optional
  "showFeedback": true,                 // optional
  "feedbackMessage": "Explanation"       // optional
}
```

⚠️ `answer` must be non-empty (`z.string().min(1)`). Empty string `""` fails validation.
⚠️ Not auto-scorable for open-ended questions — use for factual recall only.

### Multiple Response

```json
{
  "type": "multipleresponse",
  "question": "Select all that apply...",
  "options": ["A", "B", "C", "D"],     // min 2, MAX 6 options
  "correctIndices": [0, 2],             // min 2 correct, 0-based
  "showFeedback": true,                 // optional
  "feedbackMessage": "Explanation"       // optional
}
```

⚠️ Maximum 6 options — more will fail validation.
⚠️ Minimum 2 correct indices required.

### Fill in the Blank

```json
{
  "type": "fillinblank",
  "template": "The {{1}} is the {{2}} of learning.",  // use {{n}} gap markers
  "blanks": [
    { "acceptable": ["answer1", "alt1"], "caseSensitive": false }
  ],  // min 1 blank, omit id. Order matches {{n}} markers.
  "showFeedback": true  // optional
}
```

⚠️ Field is `template`, not `text`.
⚠️ No `feedbackMessage` field on content blocks — only `showFeedback`.

### Matching

```json
{
  "type": "matching",
  "prompt": "Match each term to its definition",
  "left": [{ "label": "Term 1" }, { "label": "Term 2" }],   // min 2, omit id
  "right": [{ "label": "Def 1" }, { "label": "Def 2" }],    // min 2, omit id
  "pairs": [
    { "leftIndex": 0, "rightIndex": 0 },
    { "leftIndex": 1, "rightIndex": 1 }
  ],  // min 2 pairs — use INDEX not ID
  "showFeedback": true  // optional
}
```

⚠️ `add_block` matching uses `leftIndex`/`rightIndex` (0-based integers).
⚠️ The stored schema uses `leftId`/`rightId` — IDs are injected server-side from indices.

### Sorting

```json
{
  "type": "sorting",
  "prompt": "Put these steps in order",
  "showFeedback": true,                 // REQUIRED (not optional)
  "buckets": [
    { "label": "Step 1" },
    { "label": "Step 2" },
    { "label": "Step 3" }
  ],  // min 2 buckets, omit id
  "items": [
    { "text": "Do this first", "bucketId": "<bucket-id>" }
  ]  // min 2 items, omit id
}
```

⚠️ Bucket-based architecture — NOT a simple ordered array.
⚠️ For step sequencing: create one bucket per step ("Step 1", "Step 2", etc.), one item per action, each item's `bucketId` = its correct bucket.
⚠️ `showFeedback` is REQUIRED (not optional like on other blocks).
⚠️ `bucketId` must reference a bucket — but since IDs are auto-generated, you need to either:
  - Pre-generate bucket IDs and reference them in items, OR
  - Let `injectSubItemIds` handle it (server preserves provided IDs or generates new ones)

### Hotspot

```json
{
  "type": "hotspot",
  "src": "https://image-url",
  "alt": "Image description",
  "hotspots": [
    { "x": 25, "y": 40, "label": "Feature", "description": "optional" }
  ]  // x/y are 0-100 (percentage), omit id
}
```

⚠️ Requires coordinate setup — flag for human author completion in the editor.

### Branching

```json
{
  "type": "branching",
  "prompt": "What would you do?",        // field is "prompt" NOT "scenario"
  "choices": [
    { "label": "Choice text", "content": "<p>Consequence HTML</p>" }
  ]  // min 2 choices. Fields are "label"/"content" NOT "text"/"consequence". Omit id.
}
```

⚠️ Field is `prompt`, not `scenario`.
⚠️ Choice fields are `label` and `content`, not `text` and `consequence`.
```

- [ ] **Step 2: Write the assessment question reference section**

Append to the same file. These are the schemas for `add_question` (from `mcp/src/tools/assessment.ts`). Key differences from content blocks are called out.

```markdown
---

## Assessment question schemas — `add_question`

Omit `id` on all questions and sub-items — auto-generated.
All question types use `text` for the question stem (not `question`).
All question types use a single `feedback` field (not split correct/incorrect).

### MCQ (`kind: "mcq"`)

```json
{
  "kind": "mcq",
  "text": "Question stem",               // NOT "question"
  "options": ["A", "B", "C", "D"],       // exactly 4 options (tuple)
  "correctIndex": 0,                      // 0-based, max 3
  "feedback": "Explanation",              // optional, SINGLE field
  "bloomLevel": "C",                      // optional: K|C|UN|AP|AN|EV
  "source": "LT 1.1"                     // optional: learning target reference
}
```

⚠️ Content block `quiz` uses `question` + `feedbackMessage`. Assessment MCQ uses `text` + `feedback`.

### Multiple Response (`kind: "multipleresponse"`)

```json
{
  "kind": "multipleresponse",
  "text": "Select all that apply",
  "options": ["A", "B", "C", "D"],       // min 2, MAX 6
  "correctIndices": [0, 2],              // min 2 correct
  "feedback": "Explanation"               // optional
}
```

### Fill in the Blank (`kind: "fillinblank"`)

```json
{
  "kind": "fillinblank",
  "text": "The {{1}} model has {{2}} phases.",  // NOT "template"
  "blanks": [
    { "acceptable": ["ADDIE"], "caseSensitive": false }
  ],  // omit id
  "feedback": "Explanation"               // optional
}
```

⚠️ Assessment FIB uses `text`, content block FIB uses `template`.

### Matching (`kind: "matching"`)

```json
{
  "kind": "matching",
  "text": "Match each item",
  "left": [{ "label": "Term 1" }],       // min 2, omit id
  "right": [{ "label": "Def 1" }],       // min 2, omit id
  "pairs": [
    { "leftIndex": 0, "rightIndex": 0 }
  ],  // min 2 — uses INDEX (converted to IDs server-side)
  "feedback": "Explanation"               // optional
}
```

### Sorting (`kind: "sorting"`)

```json
{
  "kind": "sorting",
  "text": "Order these steps",
  "buckets": [{ "label": "Step 1" }],    // min 2, omit id
  "items": [
    { "text": "Action", "bucketIndex": 0 }  // uses bucketINDEX not bucketId
  ],  // min 2, omit id
  "feedback": "Explanation"               // optional
}
```

⚠️ Assessment sorting uses `bucketIndex` (0-based integer). Content block sorting uses `bucketId` (string).

---

## Content block vs assessment question — field name differences

| Concept | Content block field | Assessment question field |
|---|---|---|
| Question stem | `question` | `text` |
| FIB template | `template` | `text` |
| Feedback | `feedbackMessage` (quiz, multipleresponse, shortanswer) or `feedbackCorrect`/`feedbackIncorrect` (truefalse) | `feedback` (single field, all types) |
| Matching pairs | `leftIndex`/`rightIndex` | `leftIndex`/`rightIndex` |
| Sorting item placement | `bucketId` (string) | `bucketIndex` (0-based integer) |
| Sorting feedback | `showFeedback: boolean` (required) | *(not applicable)* |

---

## `update_assessment_config`

```json
{
  "course_id": "uuid",
  "lesson_id": "uuid",
  "passingScore": 80,   // optional, 0-100
  "examSize": 10         // optional, min 1
}
```

At least one field must be provided.

> **Note:** The spec (Change 6b) mentions `timeLimit` and `maxAttempts` but the actual `update_assessment_config` tool only supports `passingScore` and `examSize`. This doc reflects the real schema.
```

- [ ] **Step 3: Write the common gotchas section**

Append to the same file:

```markdown
---

## Common gotchas

1. **Never include `id` fields** — on blocks, sub-items (accordion items, tabs items, timeline items, process steps, sorting buckets/items, branching choices, matching left/right), or questions. All auto-generated.
2. **`schemaVersion: 1`** is required on all course JSON.
3. **`shortanswer` is NOT a supported assessment question kind** — only mcq, multipleresponse, fillinblank, matching, sorting.
4. **`multipleresponse` max 6 options** — content blocks and assessment questions both enforce this.
5. **`shortanswer` answer field must be non-empty** — `answer: ""` fails `z.string().min(1)`.
6. **Audio MIME types** — `audio/mpeg` and `audio/wav` only. NOT `audio/mp3`.
7. **Document field is `src`** — not `url`.
8. **Block operations error on assessment lessons** — use `add_question`/`update_question` instead of `add_block`/`update_block`.
9. **`correctIndex` is 0-based** — factory default is -1, must be explicitly set.
10. **Position is 1-based** — when using `add_block` with position parameter.
11. **Text fields accept HTML** — use `<p>`, `<strong>`, `<em>`, etc.
12. **Sorting is bucket-based** — not a simple ordered array. Create buckets, assign items to buckets.
```

- [ ] **Step 4: Review the file for accuracy against schemas**

Read `docs/phase-3/step7-mcp-reference.md` and cross-check every field name, type, and constraint against:
- `mcp/src/lib/types.ts` (block Zod schemas, lines 316–586)
- `mcp/src/tools/assessment.ts` (question input schemas, lines 7–58)
- `mcp/src/tools/blocks.ts` (add_block tool description, lines 81–117)

Verify no field names are wrong, no required fields are marked optional, and no constraints are missing.

- [ ] **Step 5: Commit**

```bash
git add docs/phase-3/step7-mcp-reference.md
git commit -m "Add step7-mcp-reference.md — MCP schema reference for workflow builds"
```

---

### Task 2: Update `phase-3-workflow.md` — Step 3 expansion

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md:121-149` (Step 3)

- [ ] **Step 1: Replace Step 3 content**

Replace lines 121–149 (Step 3 — Content Gathering and Curation) with the expanded version. The new Step 3 must include:

1. Original items 1–3 (ask for materials, extract content, coverage matrix) — keep as-is
2. **New item 3.5: Research offer** — after the coverage matrix, for any ❌ Missing items: "I can research [topic] and draft content for this gap. Would you like me to do that, or will you provide additional materials?"
3. **New item 4: Media inventory** — add the MEDIA INVENTORY template from the spec (Change 1b)
4. **New item 5: Source content extraction** — extract text content to `docs/phase-3/source-notes-<slug>.md` (where `<slug>` is kebab-case of the working title). State this file survives compaction and session breaks.
5. **New item 6: Media note** — media assets from source materials are not stored. Step 6 will check availability and request re-uploads.
6. Replace item 4 (old "For gaps: note the LLM will draft") — removed, replaced by research offer

The PAUSE text changes from "Coverage matrix reviewed. Gaps noted." to "Coverage matrix reviewed. Research gaps addressed. Media inventory complete. Source notes saved."

- [ ] **Step 2: Verify the edit**

Read `docs/phase-3/phase-3-workflow.md` lines 121–170 to confirm the new Step 3 reads correctly and the PAUSE gate is present.

- [ ] **Step 3: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Expand Step 3 with research offer, media inventory, source extraction"
```

---

### Task 3: Update `phase-3-workflow.md` — Step 6 approval gate & content persistence

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md:305-425` (Step 6)

- [ ] **Step 1: Add build mode selection at start of Step 6**

After the current line 311 ("Work one lesson at a time..."), insert the build mode selection. Replace that line with:

```markdown
### 6-pre. Build mode and content persistence

Ask the user which mode they prefer:

- **Whole-course mode:** Draft all lessons, saving each to markdown as completed. After all lessons are drafted, present the full draft for approval.
- **Lesson-by-lesson mode (recommended for 4+ lessons):** Draft one lesson → save to markdown → present → PAUSE for approval → repeat for each lesson.

**Per-lesson save (mandatory in both modes):** After drafting each lesson, immediately save it to the build file `docs/phase-3/course-build-<slug>.md` (same slug as source notes). Do this before moving to the next lesson. If context compacts mid-step, only the lesson currently being drafted is lost.

Then work one lesson at a time through steps 6a–6g below.
```

- [ ] **Step 2: Replace the "Repeat" instruction and add approval gate + media sourcing**

Replace line 425 ("Repeat Steps 6a–6g for every lesson. After all lessons drafted, proceed.") with:

```markdown
**After each lesson:** Save the drafted content to the build markdown file immediately.

**In lesson-by-lesson mode:** Present the lesson draft and **→ PAUSE: Lesson [N] content approved?** before starting the next lesson.

**After all lessons are drafted and saved:**

### 6h. Content approval gate

Present the full draft (whole-course mode) or confirm all individual lessons are approved (lesson-by-lesson mode).

**→ PAUSE: All content approved? Proceed to media sourcing.**

### 6i. Media sourcing

1. Review the media inventory from Step 3
2. Ask the user: "Are the following source files still in our conversation? If not, please re-upload: [list files from media inventory that contained usable assets]"
3. For each media gap identified in Step 3, source or select appropriate assets (images, diagrams, videos, documents)
4. Upload assets via `upload_media` and note the returned URLs in the build markdown file
5. Update block drafts in the build file with media URLs

Default: media sourcing is batched here, after all content is approved. In lesson-by-lesson mode, the user may request per-lesson media sourcing during the lesson loop — if they do, proceed as requested.

**After media sourcing, proceed to Step 7.**
```

- [ ] **Step 3: Verify the edit**

Read `docs/phase-3/phase-3-workflow.md` lines 305–440 to confirm Step 6 flows correctly: 6-pre → 6a–6g loop → 6h approval → 6i media → Step 7.

- [ ] **Step 4: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Add Step 6 build modes, per-lesson saves, approval gate, media sourcing"
```

---

### Task 4: Update `phase-3-workflow.md` — Step 7 build method

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md:429-452` (Step 7 — approximate lines after Task 3 edits)

- [ ] **Step 1: Rewrite Step 7**

Replace the entire Step 7 section with:

```markdown
## Step 7 — Build in TideLearn

**Goal:** Persist all drafted and approved content into TideLearn via the MCP.

> **Load `step7-mcp-reference.md` now.** Use it for all field names, types, and constraints during building.

Always `get_course` before any edits to confirm current state and IDs.

**Build method:** Use `add_lesson` + `add_block` per block, built from the approved build markdown file. Do NOT use `generate_lesson` — it produces generic content that ignores drafted material.

**Build sequence:**
1. `create_course` — title, description from Step 1 brief
2. Per content lesson:
   a. `add_lesson` — title from course plan
   b. `add_block` per block in skeleton order, using exact field names from `step7-mcp-reference.md`
3. Per assessment lesson:
   a. `add_assessment_lesson` — title (NOT `add_lesson`)
   b. `add_question` per question with correct `kind` field
   c. Prompt the user for assessment config: "What passing score, exam size would you like? (Default: no minimum, all questions)"
   d. `update_assessment_config` with user-specified values
4. `upload_media` for any image, audio, or document assets not yet uploaded
5. `save_course`

**Error recovery:**
1. Read the error message — it names the exact field or constraint that failed
2. Check `step7-mcp-reference.md` for the correct schema
3. Fix the field and retry the tool call
4. After build completes, verify block count and order per lesson against the build markdown file

**Post-build verification:**
- `get_course` to confirm lesson count, block counts, and assessment question count match the approved draft
- Report any discrepancies to the user before proceeding to Step 8
```

- [ ] **Step 2: Verify the edit**

Read the Step 7 section to confirm it references `step7-mcp-reference.md`, mandates `add_lesson` + `add_block`, includes assessment config prompting, and has error recovery guidance.

- [ ] **Step 3: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Rewrite Step 7: mandate add_lesson+add_block, add MCP reference, error recovery"
```

---

### Task 5: Update `phase-3-workflow.md` — Step 8 audit re-run rule

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md` (Step 8 section, ~lines 455-500 after previous edits)

- [ ] **Step 1: Add re-run rule after Step 8c**

After the existing 8c differentiation check section (ending with "Apply adaptations via `update_block`. Then `save_course`."), insert:

```markdown
### 8d. Audit re-run rule

**Audit fixes do NOT trigger re-run.** Changes made to address findings from 8a–8c are scoped corrections the audit already identified.

**User-requested changes after Step 8 DO trigger re-run.** If the user requests changes beyond what the audit found (e.g. "add a callout here", "rewrite this quiz", "change the branching scenario"), make the changes, then inform the user: "Since the course changed beyond audit fixes, I'm re-running the quality audit." Re-run 8a–8c on affected lessons.

**Step 9 only proceeds after a clean audit state** — either the original audit + fixes, or the most recent re-run + fixes.
```

Also update the PAUSE text to reference 8d:

Change "**→ PAUSE: Audit complete. Course ready for SME review. Proceed to Step 9.**" to "**→ PAUSE: Audit complete (or re-run complete). Course ready for SME review. Proceed to Step 9.**"

- [ ] **Step 2: Verify the edit**

Read the Step 8 section to confirm 8d is present and the PAUSE text is updated.

- [ ] **Step 3: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Add Step 8d audit re-run rule: user changes trigger re-run, audit fixes don't"
```

---

### Task 6: Update `phase-3-workflow.md` — Session boundary guidance

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md` (insert after the header/vocabulary section, before Step 1)

- [ ] **Step 1: Insert session management section**

After the Vocabulary table (line 27, after `---`) and before Step 1 (line 30), insert:

```markdown
## Session Management

Large courses consume significant context window tokens. Plan session breaks to avoid losing work to compaction.

| Course size | Guidance |
|---|---|
| ≤3 lessons, ≤15 blocks/lesson | Can likely run Steps 1–9 in one session. Per-lesson save in Step 6 still mandatory. |
| 4–6 lessons | Break after Step 5. Handoff = lesson plan (Step 4g output) + source notes file (Step 3). User re-uploads source materials at start of Step 6 session. |
| 7+ lessons | Break after Step 5. Additionally, batch Step 6 into groups (e.g. lessons 1–3, then 4–7), saving each batch to the build markdown file. |

**No break between Steps 7 and 8** — they share context naturally. Step 8 needs the course state Step 7 just created.

**Handoff artifacts:**
- Step 3 → `docs/phase-3/source-notes-<slug>.md` (extracted text, survives all breaks)
- Step 5 → Course Plan Document (structured output from Step 4g)
- Step 6 → `docs/phase-3/course-build-<slug>.md` (per-lesson saves)
- Source material files (PDFs, slides, etc.) → user re-uploads at start of new session

---
```

- [ ] **Step 2: Update the header references line**

Update line 5 from:
```
**References:** Load `step4-block-planning.md` at Step 4, `step6-block-development.md` at Step 6
```
to:
```
**References:** Load `step4-block-planning.md` at Step 4, `step6-block-development.md` at Step 6, `step7-mcp-reference.md` at Step 7
```

- [ ] **Step 3: Verify the edit**

Read the top of `phase-3-workflow.md` to confirm the session management section appears between Vocabulary and Step 1, and the references line includes step7.

- [ ] **Step 4: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Add session boundary guidance and step7 reference to workflow header"
```

---

### Task 7: Update `step6-block-development.md` — Shortanswer guidance & assessment questions

**Files:**
- Modify: `docs/phase-3/step6-block-development.md:102-106` (shortanswer block section)
- Modify: `docs/phase-3/step6-block-development.md:142-157` (knowledge check selection table)
- Modify: `docs/phase-3/step6-block-development.md:172` (Bloom ceiling table — shortanswer row)

- [ ] **Step 1: Update shortanswer block description**

Replace lines 102–106 (the `shortanswer` section under Knowledge check blocks) with:

```markdown
**`shortanswer`** ⚠️ NOT auto-scorable for open-ended questions
- `acceptable`: array of acceptable answers — only works for exact-match factual answers
- `caseSensitive`: usually false
- `trimWhitespace`: usually true
- `answer` field must be non-empty (min 1 char)
- For open/reflective questions, write a substantive `feedbackMessage` — but be aware the learner's response is not auto-graded
- **Not a supported assessment question kind** — cannot be used in assessment lessons
- For auto-scorable alternatives: use `quiz` (C), `multipleresponse` (AP), or `branching` (EV)
```

- [ ] **Step 2: Update the knowledge check selection table**

Replace lines 142–157 (the selection table) with the updated version that flags shortanswer and adds auto-scorable alternatives:

```markdown
| Goal | Use | Bloom | Notes |
|---|---|---|---|
| Factual recall | `quiz`, `fillinblank`, `matching` | K | `fillinblank` stronger for vocabulary retention |
| Binary fact check | `truefalse` | K | 50% guessing chance — use sparingly |
| Concept recognition | `quiz`, `multipleresponse` | K, C | `multipleresponse` for multi-attribute concepts |
| Conceptual understanding | `quiz` (C), `multipleresponse` (AP) | C–AP | `shortanswer` also works but is not auto-scorable ⚠️ |
| Procedure sequencing | `sorting` | K, UN | Tests full sequence; use after `process` |
| Procedure application | `sorting` (UN), `quiz` with scenario stem | UN | `shortanswer` also works but is not auto-scorable ⚠️ |
| Cued recall | `fillinblank` | K, C | Use `acceptable` array for auto-scoring |
| Paired-associate recall | `matching` | K | 3–8 pairs; term ↔ definition |
| Visual identification | `hotspot` | K, UN | High authoring cost; flag for human |
| Elaborative interrogation | `quiz` with "why" stem, `multipleresponse` | C–AP | `shortanswer` also works but is not auto-scorable ⚠️ |
| Open application | `branching` | UN–EV | `shortanswer` also works but is not auto-scorable ⚠️ |
| Principles and judgement | `branching` | AP–EV | Only block that reliably reaches EV |
| Prior knowledge activation | `quiz` | K | Low stakes — lesson opener |
| Hinge point checkpoint | `quiz` (diagnostic MCQ) | K, C | Each distractor = a known misconception |
```

- [ ] **Step 3: Fix multipleresponse feedback field names**

Replace lines 108–110 (the `multipleresponse` section under Knowledge check blocks):
```markdown
**`multipleresponse`**
- All correct answers must be specified
- `feedbackCorrect` and `feedbackIncorrect` required
```

With:
```markdown
**`multipleresponse`**
- Maximum 6 options — more fails validation
- Minimum 2 correct indices required
- All correct answers must be specified via `correctIndices` (0-based)
- `feedbackMessage` (single field) — optional
```

- [ ] **Step 4: Fix D3 rule to reflect actual schema feedback support**

Replace the D3 rule (line 206–207):
```markdown
**D3: feedback is mandatory on all knowledge check blocks**
Every `quiz`, `truefalse`, `shortanswer`, `fillinblank`, `matching`, `sorting`, `multipleresponse`, and `branching` block must have feedback. No exceptions.
```

With:
```markdown
**D3: feedback is mandatory on all knowledge check blocks that support it**
- `quiz`, `truefalse`, `shortanswer`, `multipleresponse`: must have feedback text (`feedbackMessage` or `feedbackCorrect`/`feedbackIncorrect`)
- `fillinblank`, `matching`, `sorting`: set `showFeedback: true` (these blocks have no feedback text field — the platform shows correct/incorrect automatically)
- `branching`: consequence `content` on each choice serves as feedback — make it substantive
```

- [ ] **Step 5: Add assessment question type section**

After the "Development hard rules" section (after line 222, end of file), append:

```markdown
---

## Assessment question types

Assessment lessons support 5 question kinds. `shortanswer` is NOT supported in assessment lessons.

| Kind | Bloom ceiling | Best for |
|---|---|---|
| `mcq` | C | Factual recall, concept recognition, hinge-point diagnostic |
| `multipleresponse` | AP | Multi-attribute concepts, procedure checklists |
| `fillinblank` | C | Vocabulary, key terms, cued recall |
| `matching` | K | Paired-associate recall (term ↔ definition) |
| `sorting` | UN | Procedure sequencing, categorisation |

For detailed field schemas, see `step7-mcp-reference.md`.

**Key differences from content blocks:**
- Question stem field is `text` (not `question`)
- Feedback is a single `feedback` field (not `feedbackMessage` or split correct/incorrect)
- FIB uses `text` (not `template`)
- Sorting uses `bucketIndex` (not `bucketId`)

**Learning targets that require Bloom levels above the assessment ceiling (AN, EV) cannot be formally assessed** in the assessment lesson. Flag these for content-block-only assessment using `branching` or `shortanswer` blocks within content lessons.
```

- [ ] **Step 6: Verify the edits**

Read the full `step6-block-development.md` to confirm:
- Shortanswer section has the non-scorable warning
- Multipleresponse section uses `feedbackMessage` (not split correct/incorrect)
- D3 rule distinguishes feedback text blocks from showFeedback-only blocks
- Knowledge check table has updated alternatives
- New assessment question types section is at the end

- [ ] **Step 7: Commit**

```bash
git add docs/phase-3/step6-block-development.md
git commit -m "Update shortanswer guidance, add assessment question type section"
```

---

### Task 8: Update `phase-3-workflow.md` Step 4g — Assessment plan format

**Files:**
- Modify: `docs/phase-3/phase-3-workflow.md` (Step 4g assessment section, ~lines 278–281 after previous edits)

Note: The assessment plan template lives in `phase-3-workflow.md` Step 4g, not in `step4-block-planning.md`. The spec deliverables table incorrectly lists `step4-block-planning.md` for this change — `step4-block-planning.md` does not need modification.

- [ ] **Step 1: Replace the assessment section in Step 4g**

Find the assessment section in the Course Plan Document template (currently lines 278–281):
```
ASSESSMENT LESSON
  Question types: [MCQ / shortanswer / fillinblank / matching / sorting]
  Objectives covered: [all]
  Leitner spaced repetition: yes
```

Replace with:
```
ASSESSMENT LESSON
  Question count: [total]
  Per learning target:
    LT 1: [N] × [kind] — [rationale for this question type]
    LT 2: [N] × [kind] — [rationale]
    ...
  Non-assessable targets (content-block-only): [any LTs where supported question kinds cannot reach required Bloom level — these are assessed via branching/shortanswer blocks in content lessons only]
  Leitner spaced repetition: yes/no
  Supported kinds: mcq, multipleresponse, fillinblank, matching, sorting (NOT shortanswer)
```

- [ ] **Step 2: Verify the edit**

Read the Step 4g section to confirm the assessment template is updated.

- [ ] **Step 3: Commit**

```bash
git add docs/phase-3/phase-3-workflow.md
git commit -m "Expand Step 4g assessment plan with per-target question planning"
```

---

### Task 9: Update CLAUDE.md — maintenance note for step7-mcp-reference.md

**Files:**
- Modify: `CLAUDE.md` (Critical Rules section)

- [ ] **Step 1: Add maintenance note**

In the Critical Rules section of CLAUDE.md, find the bullet:
```
- When adding a new block type, update **both** `courseSchema` AND `courseSchemaPermissive` in `src/types/course.ts` — stale permissive schema causes viewer to silently show "Course not found"
```

After the second bullet ("When adding a new block type with nested items, update `injectSubItemIds`..."), add:
```
- When adding a new block type or assessment question type, update `docs/phase-3/step7-mcp-reference.md` with the new schema
```

- [ ] **Step 2: Verify the edit**

Read the Critical Rules section to confirm the new bullet is present.

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "Add step7-mcp-reference.md to block type update checklist"
```

---

### Task 10: Final review

- [ ] **Step 1: Read all 4 modified/created files end-to-end**

Read in full:
1. `docs/phase-3/step7-mcp-reference.md`
2. `docs/phase-3/phase-3-workflow.md`
3. `docs/phase-3/step6-block-development.md`
4. `CLAUDE.md` (Critical Rules section only)

Check for:
- Consistent slug naming (`<slug>` everywhere, not `<course-slug>`)
- No broken cross-references between docs
- Step numbers still sequential after insertions
- No duplicate content between `step7-mcp-reference.md` and the inline field reminders (the old Step 7 reminders should be gone)

- [ ] **Step 2: Fix any issues found**

- [ ] **Step 3: Final commit if any fixes were needed**

```bash
git add -A
git commit -m "Final review fixes for workflow revision"
```
